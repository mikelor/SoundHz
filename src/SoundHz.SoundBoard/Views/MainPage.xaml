<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:SoundHz.SoundBoard.Models"
             x:Class="SoundHz.SoundBoard.Views.MainPage"
             Title="Sound Boards">
    <Grid>
        <CollectionView ItemsSource="{Binding SoundBoards}" Margin="16" SelectionMode="Single" SelectionChanged="OnSelectionChanged">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:SoundBoardDefinition">
                    <Frame Padding="12" Margin="0,0,0,12" CornerRadius="14" HasShadow="True">
                        <VerticalStackLayout>
                            <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="18" />
                            <Label Text="{Binding Description}" FontSize="14" TextColor="Gray" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="➕"
                SemanticProperties.Description="Create sound board"
                Command="{Binding AddSoundBoardCommand}"
                WidthRequest="64"
                HeightRequest="64"
                CornerRadius="32"
                FontSize="24"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="24">
            <Button.Shadow>
                <Shadow Brush="{StaticResource PrimaryColor}" Opacity="0.6" Offset="0,6" />
            </Button.Shadow>
        </Button>
    </Grid>
</ContentPage>
